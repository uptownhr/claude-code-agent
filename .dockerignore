# Ignore everything by default
*

# Include .claude directory (agent configurations)
!.claude/
!.claude/**

# Include SDK example files
!package.json
!agent.js
!chat.js

# Include entrypoint script for docker-compose
!docker-entrypoint.sh

# Ignore local settings that shouldn't be in container
.claude/settings.local.json

# Ignore node_modules (will be installed in container)
node_modules/
